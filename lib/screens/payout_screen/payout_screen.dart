import 'package:flutter/material.dart';

class PayoutScreen extends StatelessWidget {
  static String routeName = './payout';
  const PayoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
