import 'package:flutter/material.dart';

class MenuItemsScreen extends StatelessWidget {
  static String routeName = './menuitems';
  const MenuItemsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
